#include <stdio.h>

int main()
{
    for (int megamente = 0; megamente <= 100; megamente++)
   { if (megamente %2 ==0 )
   
        {
            printf("Numero Par: %d \n", megamente);
        } else {
            printf("Numero Impar: %d \n", megamente);
        }
   } 
    return 0;
}
