#include <stdio.h>

int main()
{
    for (int imreles = 100; imreles >= 0; imreles--)
    {
        if (imreles %2 == 0)
        {
            printf("Numero Par: %d \n", imreles);
        }
        
    }
       
    return 0;
}
